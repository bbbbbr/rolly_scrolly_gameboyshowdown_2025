#ifndef _COMMON_H
#define _COMMON_H

#include <stdint.h>
#include <stdbool.h>

#define ARRAY_LEN(A)  sizeof(A) / sizeof(A[0])

typedef enum mainstates {
    STATE_PLACEHOLDER = 0
};

#endif // _COMMON_H


